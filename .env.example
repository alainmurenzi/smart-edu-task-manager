# Environment variables for SMART Edu Task Manager
# Copy this file to .env and fill in the values

# Secret key for Flask sessions (change this in production!)
SECRET_KEY=your-super-secret-key-here-generate-random-string

# Database configuration
# For local development (SQLite): comment out DATABASE_URL
# For production (Supabase): PostgreSQL connection string
# Supabase pooler connection (port 6543):
DATABASE_URL=postgresql://username:password@host-1.pooler.supabase.com:6543/postgres?sslmode=require
# Or direct connection (port 5432):
# DATABASE_URL=postgresql://username:password@host:5432/postgres?sslmode=require

# Flask Mail Configuration (optional - for email notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
